spring:
  application:
    name: account-service
  cloud:
    config:
      uri: ${CONFIG_SERVICE_ADDRESS}
      username: ${CONFIG_SERVICE_USER}
      password: ${CONFIG_SERVICE_PASSWORD}
      fail-fast: true
  main:
    allow-bean-definition-overriding: true
