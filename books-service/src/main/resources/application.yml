server:
  # servlet:
  #   context-path: 
  port: 8000

# security:
#   oauth2:
#     client:
#       clientId: books-service
#       clientSecret: ${BOOKS_SERVICE_PASSWORD}
#       accessTokenUri: http://auth-service:8000/uaa/oauth/token
#       grant-type: client_credentials
#       scope: server

spring:
  datasource:
    url: ${POSTGRESBD_URL}
    username: ${POSTGRESBD_USERNAME}
    password: ${POSTGRESBD_PASSWORD}
    driver-class-name: org.postgresql.Driver

  graphql:
    schema:
      printer:
        enabled: true
    graphiql:
      mapping: /graphiql
      enabled: true

  jpa:
    show-sql: true
    # properties:
    #   javax:
    #     persistence:
    #       schema-generation:
    #         scripts:
    #           action: create
    #           create-target: create.sql
    #           create-source: metadata


management:
  endpoints:
    web:
      exposure:
        include: "*"

            