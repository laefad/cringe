spring:
  application:
    name: ${APP_NAME}
  config:
    import: "configserver:${CONFIG_SERVICE_ADDRESS}"
  cloud:
    config:
      enabled: true
      uri: ${CONFIG_SERVICE_ADDRESS}
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: ${CONFIG_SERVICE_USER}
