spring:
  application:
    name: ${APP_NAME}
  cloud:
    config:
      uri: ${CONFIG_SERVICE_ADDRESS}
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: ${CONFIG_SERVICE_USER}
